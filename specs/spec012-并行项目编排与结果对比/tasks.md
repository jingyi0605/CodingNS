# 任务清单 - spec012-并行会话组与临时子工作区（人话版）

状态：Draft

## 这份文档是干什么的

这份清单只做一件事：
把“并行会话”拆成真正能落地的步骤，避免最后做成一堆按钮、几个标签和一团假的父子关系。

这份任务清单优先回答：

1. 并行会话到底先把什么建出来
2. 哪些是真实会话关系，哪些只是展示投影
3. 临时隔离工作区怎么接进来，怎么升级，怎么删
4. 分屏和信息栏怎么改，才不会把现有单会话体验打坏

## 状态说明

- `TODO`：还没开始
- `IN_PROGRESS`：正在做
- `BLOCKED`：被外部问题卡住
- `IN_REVIEW`：已有结果，待复核
- `DONE`：已经完成，并且已回写状态
- `CANCELLED`：取消，不做了，但必须写原因

规则：

- 只有 `状态：DONE` 的任务才能勾选成 `[x]`
- 每完成一个任务，必须立刻回写本文件
- `BLOCKED` 和 `CANCELLED` 必须写清楚原因

---

## 阶段 0：先把方向纠正，不要继续沿旧模型开发

- [x] 0.1 按最新需求重写 `spec012`
  - 状态：DONE
  - 这一步到底做什么：把旧的“并行项目尝试 / 结果对比”文档重写成“并行会话组 / 组成员 / 临时子工作区 / 分屏 UI”版本。
  - 做完以后能看到什么结果：`README.md`、`requirements.md`、`design.md`、`tasks.md` 和 `docs/README.md` 都已改成围绕并行会话的语言和模型。
  - 先依赖什么：无
  - 主要改哪里：
    - `specs/spec012-并行项目编排与结果对比/*`
    - `specs/README.md`
  - 这一步明确不做什么：不写实现代码，不补结果对比逻辑。
  - 怎么算完成：
    1. 旧的 `ParallelCase / ParallelAttempt` 被移出主模型
    2. 文档主线变成并行会话组与临时子工作区
    3. 入口、分屏和悬浮信息栏要求已写清楚
  - 怎么验证：
    - 文档自检
    - 与最新需求逐条对照

- [x] 0.2 明确真实会话关系、并行关系和临时工作区关系
  - 状态：DONE
  - 这一步到底做什么：把“真实父子会话”“并行展示关系”“临时隔离工作区”“正式子工作区”这四层关系拆开定义清楚。
  - 做完以后能看到什么结果：后面实现时不会再滥用 `parentSessionId`，也不会把临时工作区直接塞进正式子工作区树。
  - 先依赖什么：0.1
  - 主要改哪里：
    - `specs/spec012-并行项目编排与结果对比/requirements.md`
    - `specs/spec012-并行项目编排与结果对比/design.md`
  - 这一步明确不做什么：不实现具体 API。
  - 怎么算完成：
    1. 已定义 `anchorSessionId`
    2. 已定义展示投影字段
    3. 已定义临时工作区升级规则
  - 怎么验证：
    - 文档交叉检查

---

## 阶段 1：先把后端对象和关系立住

- [x] 1.1 落地并行会话组、组成员、临时隔离工作区表结构和仓储
  - 状态：DONE
  - 这一步到底做什么：新增 `parallel_session_groups`、`parallel_session_members`、`session_isolated_workspaces`，把并行关系和临时资源正式落库。
  - 做完以后能看到什么结果：系统能稳定记录一组并行会话和它们各自的临时工作区，不再靠内存拼装。
  - 先依赖什么：0.2
  - 开始前先看：
    - `requirements.md` 需求 1、需求 5、需求 6、需求 7
    - `design.md` §4
  - 主要改哪里：
    - Host 存储迁移
    - repository 文件
    - domain 类型定义
  - 这一步明确不做什么：不做前端展示，不做会话创建。
  - 怎么算完成：
    1. 三张表和仓储接口可用
    2. 锚点、成员顺序、临时工作区状态都可持久化
  - 怎么验证：
    - `parallel-session-repositories.test.ts`
    - `parallel-session-projection.test.ts`

- [x] 1.2 实现从现有会话创建并行组的后端编排
  - 状态：DONE
  - 这一步到底做什么：新增“从现有会话或消息发起并行 fork”的服务和接口。
  - 做完以后能看到什么结果：用户不再需要连点多次 fork 才能凑出并行组。
  - 先依赖什么：1.1
  - 开始前先看：
    - `requirements.md` 需求 2、需求 4
    - `design.md` §5.1、§7.1
  - 主要改哪里：
    - 并行组 service
    - session controller / assistant capability route
    - fork 链路复用与错误回填
  - 这一步明确不做什么：不做新建工作区入口。
  - 怎么算完成：
    1. 能从会话发起并行组
    2. 每个成员保留真实 fork 元数据
    3. 失败精确落到成员级别
  - 怎么验证：
    - `parallel-session-group-service.test.ts`
    - `pnpm -C apps/host exec tsc --noEmit`

- [x] 1.3 实现从工作区直接新建并行组的后端编排
  - 状态：DONE
  - 这一步到底做什么：新增“从工作区直接创建并行会话组”的服务和接口。
  - 做完以后能看到什么结果：用户能一步建出多条并行新会话，不再先建一条再重复补。
  - 先依赖什么：1.1
  - 开始前先看：
    - `requirements.md` 需求 3、需求 4
    - `design.md` §5.2、§7.2
  - 主要改哪里：
    - 并行组 service
    - workspace 相关入口
    - 会话启动链路复用
  - 这一步明确不做什么：不做分屏 UI。
  - 怎么算完成：
    1. 能从工作区一次创建 2 到 4 条并行会话
    2. 组内成员不伪造真实父会话
    3. 能稳定回填 `anchorSessionId`
  - 怎么验证：
    - `parallel-session-group-service.test.ts`
    - 锚点删除重选测试已覆盖

- [x] 1.4 给工作台 DTO 补并行投影字段
  - 状态：DONE
  - 这一步到底做什么：在不改坏真实会话字段的前提下，补 `parallelGroup`、`displayParentSessionId` 和临时工作区摘要。
  - 做完以后能看到什么结果：前端能看清并行关系，但底层 fork 深度和真实血缘不被污染。
  - 先依赖什么：1.2、1.3
  - 开始前先看：
    - `requirements.md` 需求 8、需求 11
    - `design.md` §4.4
  - 主要改哪里：
    - workbench assembler
    - session list DTO
    - realtime snapshot projection
  - 这一步明确不做什么：不实现最终 UI。
  - 怎么算完成：
    1. DTO 已有并行摘要
    2. 展示投影和真实父子字段分离
  - 怎么验证：
    - `parallel-session-projection.test.ts`
    - `workbench-service.test.ts`

---

## 阶段 2：把临时隔离工作区接进来，但别污染正式子工作区

- [x] 2.1 实现成员级临时隔离工作区创建
  - 状态：DONE
  - 这一步到底做什么：给启用隔离的并行成员创建真实 Git worktree，并把它登记为会话级临时资源。
  - 做完以后能看到什么结果：某些并行会话可以独立代码目录工作，但不会直接混入正式子工作区树。
  - 先依赖什么：1.1、1.2、1.3
  - 开始前先看：
    - `requirements.md` 需求 5
    - `design.md` §4.3、§6.3
    - `spec012.1`
  - 主要改哪里：
    - session isolated workspace service
    - Git worktree 底层复用
    - workspace import 逻辑
  - 这一步明确不做什么：不做正式升级。
  - 怎么算完成：
    1. 启用隔离的成员能创建临时工作区
    2. 不启用隔离的成员不会误建 worktree
    3. 临时工作区不会出现在正式子工作区树
  - 怎么验证：
    - `session-isolated-workspace-service.test.ts`
    - 顶层工作区隐藏测试已覆盖

- [x] 2.2 实现临时隔离工作区升级为正式子工作区
  - 状态：DONE
  - 这一步到底做什么：把会话级临时资源转成现有正式子工作区。
  - 做完以后能看到什么结果：有价值的临时工作区能继续长期管理，不会因为会话删除自动丢掉。
  - 先依赖什么：2.1
  - 开始前先看：
    - `requirements.md` 需求 6
    - `design.md` §6.4
    - `spec012.1`
  - 主要改哪里：
    - 升级接口
    - worktree 元数据接入
    - 工作台展示切换逻辑
  - 这一步明确不做什么：不做自动 merge。
  - 怎么算完成：
    1. 可手动升级
    2. 升级后进入正式子工作区管理
    3. 原临时记录状态正确变更
  - 怎么验证：
    - `session-isolated-workspace-service.test.ts`
    - `POST /api/session-isolated-workspaces/:id/promote` 已接入

- [x] 2.3 实现删除并行成员和整组时的临时资源清理
  - 状态：DONE
  - 这一步到底做什么：删除成员或整组时，自动清理未升级的临时工作区和分支。
  - 做完以后能看到什么结果：并行会话用完能收干净，不会越跑越脏。
  - 先依赖什么：2.1、2.2
  - 开始前先看：
    - `requirements.md` 需求 7
    - `design.md` §6.5
  - 主要改哪里：
    - 会话删除钩子
    - 组删除 service
    - cleanup 逻辑
  - 这一步明确不做什么：不清理已升级的正式子工作区。
  - 怎么算完成：
    1. 删成员能清掉对应临时资源
    2. 删整组能批量清理
    3. 清理失败会留下明确残留信息
  - 怎么验证：
    - `session-isolated-workspace-service.test.ts`
    - `parallel-session-group-service.test.ts`

---

## 阶段 3：把前端入口、分屏和悬浮信息栏落地

- [ ] 3.1 实现并行创建入口和弹窗
  - 状态：IN_REVIEW
  - 这一步到底做什么：在 fork 按钮旁和“新建工作区”左侧接入并行创建入口，并落地创建弹窗。
  - 做完以后能看到什么结果：用户能从对的地方进入并行流程，而不是靠隐藏接口。
  - 先依赖什么：1.2、1.3
  - 开始前先看：
    - `requirements.md` 需求 4
    - `design.md` §8.1、§8.2
    - 页面与模态框设计规范
  - 主要改哪里：
    - `apps/user-app` 会话操作区
    - 新建会话入口
    - 并行创建弹窗
  - 这一步明确不做什么：不做分屏内容渲染。
  - 怎么算完成：
    1. 两个入口都可见
    2. 弹窗支持公共配置和成员级配置
    3. 校验提示明确到成员级别
  - 怎么验证：
    - 组件测试
    - 手工回放

- [ ] 3.2 实现会话树里的并行标签、组颜色和临时工作区提示
  - 状态：IN_REVIEW
  - 这一步到底做什么：让工作台、会话树和连接线能看清哪些会话属于一组并行，以及哪条会话挂了临时隔离工作区。
  - 做完以后能看到什么结果：用户不用猜哪几条是一组，也不用猜哪个目录挂在哪个会话下。
  - 先依赖什么：1.4、2.1
  - 开始前先看：
    - `requirements.md` 需求 2、需求 8
    - `design.md` §4.4、§8
  - 主要改哪里：
    - workbench 会话树
    - session header
    - 连接线样式和颜色 token
  - 这一步明确不做什么：不改正式子工作区树结构。
  - 怎么算完成：
    1. 并行标签和颜色稳定可见
    2. 展示关系正确
    3. 临时工作区提示挂在对应会话上下文里
  - 怎么验证：
    - 组件测试
    - 树结构快照测试

- [ ] 3.3 实现中间区域并行分屏布局
  - 状态：IN_REVIEW
  - 这一步到底做什么：创建并行组后，把中间区域切成 2 到 4 屏并排布局。
  - 做完以后能看到什么结果：用户一创建并行组，就能同时看多条会话，不用来回切。
  - 先依赖什么：1.4
  - 开始前先看：
    - `requirements.md` 需求 9
    - `design.md` §8.3
    - 页面样式设计规范
  - 主要改哪里：
    - conversation layout
    - parallel view state
    - 分屏响应式样式
  - 这一步明确不做什么：不恢复全局右侧信息栏。
  - 怎么算完成：
    1. 2 到 4 屏可正常渲染
    2. 每屏信息密度明显收紧
    3. 锚点和顺序可恢复
  - 怎么验证：
    - 组件测试
    - 桌面宽度回放

- [ ] 3.4 实现每屏顶部按钮触发的悬浮信息栏
  - 状态：IN_REVIEW
  - 这一步到底做什么：把原全局右侧信息栏改成分屏顶部按钮触发的局部浮层。
  - 做完以后能看到什么结果：信息按需看，不再一直挤占所有并行分屏的宽度。
  - 先依赖什么：3.3
  - 开始前先看：
    - `requirements.md` 需求 10
    - `design.md` §8.4
    - 模态框与按钮设计规范
  - 主要改哪里：
    - conversation info panel
    - 分屏顶部工具条
    - 浮层定位和关闭逻辑
  - 这一步明确不做什么：不改变普通单会话下的信息栏默认行为。
  - 怎么算完成：
    1. 并行分屏下默认隐藏全局信息栏
    2. 每屏可单独打开自己的浮层
    3. 打开一个不影响其他分屏当前状态
  - 怎么验证：
    - 组件测试
    - 交互回放

---

## 阶段 4：回归、验证和收口

- [ ] 4.1 端到端回放两种创建路径
  - 状态：TODO
  - 这一步到底做什么：完整回放“从现有会话并行 fork”和“从工作区新建并行组”两条主链路。
  - 做完以后能看到什么结果：能确认并行会话不是只在一种路径上能跑。
  - 先依赖什么：3.4
  - 开始前先看：
    - `requirements.md` 需求 2、需求 3、需求 4、需求 9、需求 10
  - 主要改哪里：测试和验收文档
  - 这一步明确不做什么：不做结果优劣比较。
  - 怎么算完成：
    1. 两条主链路都能回放
    2. 锚点、分屏和信息栏行为稳定
  - 怎么验证：
    - 集成测试
    - 人工回放

- [ ] 4.2 回放临时工作区的创建、升级和删除
  - 状态：TODO
  - 这一步到底做什么：验证临时隔离工作区从创建到升级、再到删除时的资源边界。
  - 做完以后能看到什么结果：能确认临时资源不会误混正式子工作区，也不会误删升级后的资产。
  - 先依赖什么：2.3、3.4
  - 开始前先看：
    - `requirements.md` 需求 5、需求 6、需求 7
  - 主要改哪里：测试和验收文档
  - 这一步明确不做什么：不做自动合并。
  - 怎么算完成：
    1. 临时创建可用
    2. 升级后展示切换正确
    3. 删除规则符合预期
  - 怎么验证：
    - worktree 集成测试
    - 人工验收清单

- [ ] 4.3 普通单会话链路零破坏回归
  - 状态：TODO
  - 这一步到底做什么：确认新建普通会话、普通 fork、正式子工作区和旧的信息栏主链路没被并行能力打坏。
  - 做完以后能看到什么结果：用户不使用并行功能时，几乎感知不到任何行为变化。
  - 先依赖什么：全部实现任务
  - 开始前先看：
    - `requirements.md` 需求 11
  - 主要改哪里：测试和验收文档
  - 这一步明确不做什么：不扩展新功能。
  - 怎么算完成：
    1. 现有单会话主链路稳定
    2. 正式子工作区主链路稳定
    3. 错误被限制在组或成员范围内
  - 怎么验证：
    - 回归测试
    - 手工冒烟测试
