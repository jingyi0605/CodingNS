# 任务清单 - spec004.2.1-演示文稿视图布局编辑一期（人话版）

状态：Draft

## 2026-05-16 立项补记

- 已确认这次不是重做 `spec004.2`，而是补“布局编辑一期”。
- 已确认本轮范围只收敛到三件事：单节点移动、单节点缩放、多选后左/右/顶/底对齐。
- 已确认第一阶段不能碰所有节点，只能先做几何可写节点。
- 已确认保存必须继续复用现有 HTML 回写和文件版本保护链路。
- 已完成本 Spec 的 `README.md`、`requirements.md`、`design.md`、`tasks.md` 初始化。

## 阶段 0：把边界写死，别一上来就过度设计

- [x] 0.1 建立 `spec004.2.1` 文档骨架并写清范围
  - 状态：DONE
  - 这一步到底做什么：创建子 Spec，把“布局编辑一期”的目标、边界和不做事项先钉死。
  - 做完你能看到什么：后面的人不会把这个需求误写成“重做一个 PPT 编辑器”。
  - 先依赖什么：无
  - 开始前先看：
    - `specs/spec004.2-静态HTML演示文档编辑器与导出能力/README.md`
    - `specs/spec004.2-静态HTML演示文档编辑器与导出能力/requirements.md`
    - `specs/spec004.2-静态HTML演示文档编辑器与导出能力/design.md`
  - 主要改哪里：
    - `specs/spec004.2.1-演示文稿视图布局编辑一期/README.md`
    - `specs/spec004.2.1-演示文稿视图布局编辑一期/requirements.md`
    - `specs/spec004.2.1-演示文稿视图布局编辑一期/design.md`
    - `specs/spec004.2.1-演示文稿视图布局编辑一期/tasks.md`
  - 这一步先不做什么：不写实现代码，不补交互细节，不做技术选型试验。
  - 怎么算完成：
    1. 已写清楚本轮覆盖什么
    2. 已写清楚本轮明确不做什么
    3. 已把任务拆成可执行阶段
  - 怎么验证：
    - 人工走查 Spec 文档
  - 对应需求：`requirements.md` 全部需求
  - 对应设计：`design.md` 全文
  - 完成记录：
    - 已建立 `spec004.2.1` 目录和四份主文档。
    - 已把范围收敛到“布局编辑模式、单节点拖动、单节点缩放、多选对齐”。
    - 已明确不做旋转、分布、吸附线、复杂网页布局重写。

## 阶段 1：先把“哪些节点能做布局编辑”钉死

- [x] 1.1 盘点现有节点类型，定出布局编辑准入规则
  - 状态：DONE
  - 这一步到底做什么：把当前演示文稿视图里会出现的节点类型重新过一遍，明确哪些节点允许拖动、缩放和参与对齐。
  - 做完你能看到什么：一份清楚的节点准入表，后面不再靠 if/else 乱补。
  - 先依赖什么：0.1
  - 开始前先看：
    - `requirements.md` 需求 2
    - `design.md` §3.3「节点准入规则」
    - `specs/spec004.2-静态HTML演示文档编辑器与导出能力/docs/20260515-页面模型与回写约束.md`
  - 主要改哪里：
    - `specs/spec004.2.1-演示文稿视图布局编辑一期/docs/20260516-布局编辑一期边界与节点准入说明.md`
    - 如有必要同步补充 `design.md`
  - 这一步先不做什么：不做 UI，不做拖拽手势。
  - 怎么算完成：
    1. 已列出允许布局编辑的节点类型
    2. 已列出禁止布局编辑的节点类型
    3. 已写清楚准入原因和保存约束
  - 怎么验证：
    - 人工走查现有样板 HTML 与节点模型
  - 对应需求：`requirements.md` 需求 2
  - 对应设计：`design.md` §3.3、§4.4
  - 完成记录：
    - 已补充 `docs/20260516-布局编辑一期边界与节点准入说明.md`，明确只有几何可稳定写回 HTML 的节点才允许进入布局编辑。
    - 已在实现里收紧准入，排除页根、只读 `svg/html/decoration` 和不具备稳定几何写回条件的节点。

- [x] 1.2 补齐布局模式和多选状态模型
  - 状态：DONE
  - 这一步到底做什么：把单选文本编辑状态扩成布局模式、多选集合和当前几何操作状态。
  - 做完你能看到什么：后续拖动、缩放、对齐都围绕一套状态模型转，不再各写各的局部状态。
  - 先依赖什么：1.1
  - 开始前先看：
    - `requirements.md` 需求 1、需求 5、需求 7
    - `design.md` §3.2「新增视图状态」
  - 主要改哪里：
    - `apps/user-app/src/features/static-html-editor/StaticHtmlPresentationView.tsx`
    - `apps/user-app/src/features/static-html-editor/` 下新增状态辅助模块（如需要）
  - 这一步先不做什么：不直接上拖拽手势，不做对齐按钮。
  - 怎么算完成：
    1. 已有 `content / layout` 模式切换
    2. 已有稳定的 `selectedNodeIds`
    3. 切页后不会残留跨页多选
  - 怎么验证：
    - `vitest` 组件状态测试
    - 人工切页和多选验证
  - 对应需求：`requirements.md` 需求 1、需求 5、需求 7
  - 对应设计：`design.md` §2.3、§3.2、§4.3
  - 完成记录：
    - 已补齐 `content / layout` 模式切换、`selectedNodeIds` 多选集合和 `layoutGesture` 几何操作状态。
    - 已接入 iframe 与外层视图的 `layoutSelectedNodeIds` 同步，切页时会自动清理跨页选区。

### 阶段检查

- [x] 1.3 节点准入和状态模型检查
  - 状态：DONE
  - 这一步到底做什么：确认后续布局编辑不会建立在一堆模糊状态上。
  - 做完你能看到什么：可以进入拖拽和缩放实现，而不是边写边改基础状态。
  - 先依赖什么：1.1、1.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
  - 主要改哪里：本阶段全部相关文件
  - 这一步先不做什么：不扩新范围。
  - 怎么算完成：
    1. 节点准入规则已固定
    2. 多选和模式状态已能支撑后续实现
  - 怎么验证：
    - 人工走查
  - 对应需求：`requirements.md` 需求 1、需求 2、需求 5
  - 对应设计：`design.md` §3.2、§3.3
  - 完成记录：
    - 已把节点准入收敛到统一的 `resolveLayoutCapability(...)`。
    - 已确认布局模式、多选状态和准入限制围绕同一套状态模型运行，没有再长第二套补丁状态。

## 阶段 2：把单节点布局编辑跑通

- [x] 2.1 实现单节点拖动位置
  - 状态：DONE
  - 这一步到底做什么：让布局可编辑节点可以在画布里直接拖动，不再只靠 `X / Y` 输入框。
  - 做完你能看到什么：用户能拖着组件走，放开后位置稳定留在草稿状态里。
  - 先依赖什么：1.3
  - 开始前先看：
    - `requirements.md` 需求 3
    - `design.md` §2.3.2「拖动或缩放单节点」
    - `design.md` §4.2「拖动与缩放策略」
  - 主要改哪里：
    - `apps/user-app/src/features/static-html-editor/StaticHtmlPresentationView.tsx`
    - `apps/user-app/src/features/static-html-editor/` 下几何更新模块
    - `apps/user-app/src/app/styles.css`
  - 这一步先不做什么：不做多选，不做对齐。
  - 怎么算完成：
    1. 单节点可以拖动
    2. 拖动结束后 `box.x / box.y` 更新
    3. 操作能进入撤销历史
  - 怎么验证：
    - `vitest` 几何计算测试
    - 人工拖动验证
  - 对应需求：`requirements.md` 需求 3
  - 对应设计：`design.md` §4.1、§4.2、§6.1
  - 完成记录：
    - 已接入布局 overlay 和单节点拖动。
    - 拖动过程实时更新 `box.x / box.y`，放开指针后会统一进入撤销历史。

- [x] 2.2 实现单节点缩放
  - 状态：DONE
  - 这一步到底做什么：给布局可编辑节点补尺寸手柄，支持直接拉伸宽高。
  - 做完你能看到什么：用户能在画布里直接改大小，而不是再回去敲宽高字段。
  - 先依赖什么：2.1
  - 开始前先看：
    - `requirements.md` 需求 4
    - `design.md` §4.2「拖动与缩放策略」
  - 主要改哪里：
    - `apps/user-app/src/features/static-html-editor/StaticHtmlPresentationView.tsx`
    - `apps/user-app/src/features/static-html-editor/` 下几何更新模块
    - `apps/user-app/src/app/styles.css`
  - 这一步先不做什么：不做旋转、不做比例锁定。
  - 怎么算完成：
    1. 已有最小可用的尺寸手柄
    2. 缩放后 `box.width / box.height` 更新
    3. 非法最小尺寸有保护
  - 怎么验证：
    - `vitest` 几何计算测试
    - 人工缩放验证
  - 对应需求：`requirements.md` 需求 4
  - 对应设计：`design.md` §4.2、§5.2、§6.1
  - 完成记录：
    - 已补尺寸手柄，当前支持右下角缩放。
    - 缩放结果会更新 `box.width / box.height`，并通过 `clampLayoutBox(...)` 约束最小尺寸。

### 阶段检查

- [x] 2.3 单节点布局编辑检查
  - 状态：DONE
  - 这一步到底做什么：确认拖动和缩放已经是真的可保存能力，不是表演。
  - 做完你能看到什么：单节点布局编辑可以独立站稳。
  - 先依赖什么：2.1、2.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
  - 主要改哪里：本阶段全部相关文件
  - 这一步先不做什么：不抢着上多选对齐。
  - 怎么算完成：
    1. 拖动和缩放能保存
    2. 重开文件后结果稳定
  - 怎么验证：
    - 人工保存回放
    - 关键测试验证
  - 对应需求：`requirements.md` 需求 3、需求 4
  - 对应设计：`design.md` §4.2、§6.1
  - 完成记录：
    - 已修复布局表单直接清空输入导致宽高串值的问题，改为“草稿输入 + 提交落盘”。
    - 已验证单节点位置和尺寸修改可以保存回 HTML。

## 阶段 3：把多选和对齐补齐

- [x] 3.1 实现同页多选
  - 状态：DONE
  - 这一步到底做什么：让用户可以在同一页追加选择多个节点，为对齐操作提供基础。
  - 做完你能看到什么：画布和面板里能明确看到当前多选集合。
  - 先依赖什么：2.3
  - 开始前先看：
    - `requirements.md` 需求 5
    - `design.md` §3.2「新增视图状态」
    - `design.md` §4.3「多选策略」
  - 主要改哪里：
    - `apps/user-app/src/features/static-html-editor/StaticHtmlPresentationView.tsx`
    - 相关测试文件
  - 这一步先不做什么：不做框选。
  - 怎么算完成：
    1. 已支持追加选择和取消选择
    2. 只允许同页多选
    3. 切页后选区会正确清理
  - 怎么验证：
    - `vitest` 状态测试
    - 人工多选验证
  - 对应需求：`requirements.md` 需求 5
  - 对应设计：`design.md` §3.2、§4.3、§6.2
  - 完成记录：
    - 已支持组件列表和画布中的追加多选、取消选择。
    - 已补 iframe 选择桥的 `metaKey / ctrlKey` 透传，保证布局模式下追加选择稳定工作。

- [x] 3.2 实现左/右/顶/底对齐
  - 状态：DONE
  - 这一步到底做什么：基于多选集合提供四个基础对齐命令。
  - 做完你能看到什么：多个组件可以一键对齐，不再手工抠像素。
  - 先依赖什么：3.1
  - 开始前先看：
    - `requirements.md` 需求 6
    - `design.md` §3.4「对齐基准」
  - 主要改哪里：
    - `apps/user-app/src/features/static-html-editor/StaticHtmlPresentationView.tsx`
    - `apps/user-app/src/features/static-html-editor/` 下几何更新模块
    - `apps/user-app/src/app/styles.css`
  - 这一步先不做什么：不做等距分布和智能吸附。
  - 怎么算完成：
    1. 已支持左对齐、右对齐、顶对齐、底对齐
    2. 对齐只改几何字段
    3. 对齐结果可以保存并重开验证
  - 怎么验证：
    - `vitest` 几何对齐测试
    - 人工操作验证
  - 对应需求：`requirements.md` 需求 6
  - 对应设计：`design.md` §3.4、§4.4、§6.1
  - 完成记录：
    - 已实现左对齐、右对齐、顶对齐、底对齐。
    - 对齐只改 `DocumentNode.box`，并继续复用现有 HTML 回写链路保存。

### 最终检查

- [x] 3.3 布局编辑一期最终检查
  - 状态：DONE
  - 这一步到底做什么：确认这轮交付的是一条完整主链路，而不是半截交互。
  - 做完你能看到什么：单节点移动、单节点缩放、多选对齐全部可保存、可重开、可回归。
  - 先依赖什么：3.1、3.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
    - `docs/20260516-布局编辑一期边界与节点准入说明.md`
  - 主要改哪里：当前 Spec 全部文件和实现文件
  - 这一步先不做什么：不再追加新交互。
  - 怎么算完成：
    1. 三类目标能力全部跑通
    2. 不支持节点限制清楚
    3. 现有文字编辑、保存、导出没有回退
  - 怎么验证：
    - 按 Spec 验收清单逐项核对
    - 关键自动化测试
    - 人工保存重开回放
  - 对应需求：`requirements.md` 全部需求
  - 对应设计：`design.md` 全文
  - 完成记录：
    - 已完成布局编辑模式、单节点拖动、单节点缩放、同页多选和基础对齐。
    - 已通过布局模块、解析模块和文件查看器布局相关用例验证。
    - 已确认 `apps/user-app` 当前 `tsc --noEmit` 通过。
