# 任务清单 - spec016-代码视图与事务视图双工作台重构（人话版）

状态：DONE

## 这份文档是干什么的

这份任务清单只做一件事：

把这次“双工作台重构”拆成可落地的阶段，避免最后变成：

- 顶层名字改了，但骨架没改
- 文档库塞进来了，但事务视图没立住
- 桌面端能切，移动端直接坏掉
- 代码视图莫名其妙被牵连回归

## 状态说明

- `TODO`：还没开始
- `IN_PROGRESS`：正在做
- `BLOCKED`：被外部问题卡住
- `IN_REVIEW`：已经有结果，待复核
- `DONE`：已经完成，并且已回写状态
- `CANCELLED`：取消，不做了，但必须写原因

规则：

- 只有 `状态：DONE` 的任务才能勾选成 `[x]`
- 每完成一个任务，必须立刻回写本文件
- `BLOCKED` 和 `CANCELLED` 必须写清原因

---

## 阶段 0：先把 Spec 和顶层命名定住

- [x] 0.1 建立 `spec016` 文档骨架并锁定主问题
  - 状态：DONE
  - 这一步到底做什么：创建 `README.md`、`requirements.md`、`design.md`、`tasks.md` 和 `docs/README.md`，把这次“双工作台重构”从聊天结论变成正式 Spec。
  - 做完你能看到什么：仓库里出现完整 `spec016` 目录，主问题、边界、依赖和阶段安排都已明确。
  - 先依赖什么：无
  - 开始前先看：
    - `spec003`
    - `spec009`
    - `spec009.1`
    - `spec013.5`
    - `spec015`
  - 主要改哪里：
    - `specs/spec016-代码视图与事务视图双工作台重构/*`
    - `specs/README.md`
  - 这一步先不做什么：不改任何前端实现代码，不提前建半成品页面。
  - 怎么算完成：
    1. `spec016` 主文档齐全
    2. 已明确这是顶层模式重构，不是文案替换
    3. 已明确代码视图和事务视图的主边界
  - 怎么验证：
    - 文档走查
  - 对应需求：`requirements.md` 全部需求
  - 对应设计：`design.md` 全文

- [x] 0.2 写死顶层命名结论：`代码 / 事务`
  - 状态：DONE
  - 这一步到底做什么：把第二个顶层视图的命名从模糊“办公”收敛成正式“事务”，避免后面 UI、路由、文案到处摇摆。
  - 做完你能看到什么：Spec 文档里已经统一采用“代码 / 事务”，后续设计和实现不再围绕名字反复拉扯。
  - 先依赖什么：0.1
  - 开始前先看：
    - `requirements.md` 需求 2
    - `design.md` §1、§2
  - 主要改哪里：
    - `specs/spec016-代码视图与事务视图双工作台重构/README.md`
    - `specs/spec016-代码视图与事务视图双工作台重构/requirements.md`
    - `specs/spec016-代码视图与事务视图双工作台重构/design.md`
  - 这一步先不做什么：不提前细抠图标、颜色和按钮样式。
  - 怎么算完成：
    1. 第二个顶层视图的正式命名已统一
    2. 已明确“事务”覆盖办公、生活、学习、待办、自动化和插件
  - 怎么验证：
    - 文档交叉检查
  - 对应需求：`requirements.md` 需求 2
  - 对应设计：`design.md` §1.1、§2.1

---

## 阶段 1：先把顶层模式和路由骨架立住

- [x] 1.1 把现有左上角入口抽成正式顶层模式切换
  - 状态：DONE
  - 这一步到底做什么：把现在的左上角“对话 / 助手”等入口，从旧的中心区切换语义中拆出来，升级成正式“代码 / 事务”顶层模式切换，并明确助手不再占顶层导航。
  - 本次实现结果：桌面端左侧原 `对话 / 助手` 成对按钮位已直接替换成正式 `代码 / 事务` 顶层切换，继续沿用原 segmented 样式；代码模式下保留 `对话 / 助手 / 终端` 作为下方代码入口，事务模式下隐藏这组代码内部分栏入口。
  - 做完你能看到什么：桌面端主工作台左上角出现正式的顶层双视图入口，不再只是旧 center tab 变体，而且两个视图继续共享同一套顶层壳层。
  - 先依赖什么：阶段 0
  - 开始前先看：
    - `requirements.md` 需求 1、需求 2
    - `design.md` §2.1、§2.3.1、§3.1
    - `apps/user-app/src/features/conversation/components/WorkbenchLayout.tsx`
  - 主要改哪里：
    - `apps/user-app/src/features/conversation/components/WorkbenchLayout.tsx`
    - 新增或拆分相关模式切换组件
    - `apps/user-app/src/shared/i18n/index.ts`
  - 这一步先不做什么：不改代码视图内部三栏结构，不接事务视图真实内容，也不删除现有 Butler 页面。
  - 怎么算完成：
    1. 顶层模式切换已可见
    2. 切换状态可在当前工作区维度保存和恢复
  - 怎么验证：
    - 桌面端切换测试
    - i18n 文案检查
  - 对应需求：`requirements.md` 需求 1、需求 2
  - 对应设计：`design.md` §2.1、§3.1、§4.2

- [x] 1.2 建立顶层模式状态模型和兼容路由策略
  - 状态：DONE
  - 这一步到底做什么：定义 `code / affairs` 的状态保存、路由恢复和老入口兼容规则，避免模式切换后状态串线。
  - 本次实现结果：已新增 `/workspaces/:workspaceId/affairs`；老代码路径继续归 `code`；事务路径自动归 `affairs`；当前工作区模式、各模式最近路径、事务局部状态已经单独持久化。
  - 做完你能看到什么：刷新、切工作区、返回上一步时，系统知道该恢复哪个顶层模式和哪个局部状态。
  - 先依赖什么：1.1
  - 开始前先看：
    - `requirements.md` 需求 1、需求 9
    - `design.md` §3.2、§4.1、§4.2、§5.3
  - 主要改哪里：
    - `apps/user-app` 工作台路由和状态存储相关代码
    - 相关导航工具模块
  - 这一步先不做什么：不细抠事务视图内部业务数据。
  - 怎么算完成：
    1. 顶层模式状态不串工作区
    2. 老入口和老路径有明确兼容方案
  - 怎么验证：
    - 切换工作区和刷新回放测试
    - 路由兼容测试
  - 对应需求：`requirements.md` 需求 1、需求 9
  - 对应设计：`design.md` §3.2、§4.1、§4.2、§6.3

### 阶段检查

- [x] 1.3 顶层模式阶段检查
  - 状态：DONE
  - 这一步到底做什么：只检查顶层模式是否已经站稳，不带着结构性问题进入下一阶段。
  - 本次检查结果：`apps/user-app` 已能在旧代码路由和新 affairs 路由之间切换；`pnpm --dir apps/user-app build` 已通过；但事务视图移动端降级、更多对象分区和更细的联动验证还在后续阶段。
  - 做完你能看到什么：可以继续做事务视图，而不是还在靠旧入口和临时状态凑合。
  - 先依赖什么：1.1、1.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
  - 主要改哪里：本阶段全部相关文件
  - 这一步先不做什么：不追加事务视图细节功能。
  - 怎么算完成：
    1. 顶层模式切换稳定
    2. 状态恢复和兼容路径清楚
  - 怎么验证：
    - 人工走查
    - 关键切换链路回放
  - 对应需求：`requirements.md` 需求 1、需求 2、需求 9
  - 对应设计：`design.md` §2.3.1、§4.2、§6.3

---

## 阶段 2：代码视图零破坏收口

- [x] 2.1 把现有代码工作台封装成正式 `代码视图`
  - 状态：DONE
  - 这一步到底做什么：把现有三栏工作台收口成正式代码视图容器，让它成为双视图中的稳定一支。
  - 本次实现结果：新增 `CodeWorkbenchView`，先用最薄封装把现有 `Outlet` 包起来，不碰消息流、Git、终端和右侧工具主链路。
  - 做完你能看到什么：代码视图在顶层模式下仍保持原有会话、消息和工具体验，不再受事务视图施工干扰。
  - 先依赖什么：1.3
  - 开始前先看：
    - `requirements.md` 需求 3
    - `design.md` §2.2、§4.1、§6.1
    - `apps/user-app/src/features/conversation/components/WorkbenchLayout.tsx`
  - 主要改哪里：
    - `apps/user-app/src/features/conversation/components/WorkbenchLayout.tsx`
    - 可能拆出的代码视图容器组件
  - 这一步先不做什么：不重写消息流、不重构 Git、不动终端主链路。
  - 怎么算完成：
    1. 代码视图主结构保持不变
    2. 主要代码工作流能继续跑通
  - 怎么验证：
    - 现有工作台回归测试
    - 人工回归关键路径
  - 对应需求：`requirements.md` 需求 3
  - 对应设计：`design.md` §2.2、§4.1、§6.1

- [x] 2.2 收紧旧 center tab 的职责边界
  - 状态：DONE
  - 这一步到底做什么：清理旧中心 tab 和新顶层模式之间的职责重叠，避免后面两个层级互相打架。
  - 本次实现结果：原 `对话 / 助手` 顶部 paired 区已改为 `代码 / 事务`；代码视图顶部入口继续收紧，只保留 `对话 / 终端 / 技能`，移除左侧 `助手` 按钮；搜索入口移到全局顶部工具区，放在通知按钮右侧，只显示搜索图标；顶层 `代码 / 事务` 进一步改成更像 Switch 的文字标签切换，不再显示图标，并继续把边框、背景、激活态厚度和整体尺寸都压轻；同时把整个标签切换区整体上提，尽量贴近顶部工具栏，把左侧顶部观感收成“工具栏 + 模式与代码入口”两层，而不是三层；`对话 / 终端 / 技能` 三个纵向入口的图标线宽、占位宽度和视觉重心也已统一，并把代码入口默认态和激活态都降到更轻的层级，让顶层模式切换和代码入口既分组明确，又不互相抢视觉；顶层 `代码 / 事务` 负责切整个工作台，不再和旧 center tab 混层。
  - 做完你能看到什么：顶层切顶层，代码视图内部切代码视图内部，不再混层。
  - 先依赖什么：2.1
  - 开始前先看：
    - `requirements.md` 需求 1、需求 3、需求 8
    - `design.md` §2.1、§2.2、§6.1
  - 主要改哪里：
    - `apps/user-app/src/features/conversation/components/WorkbenchLayout.tsx`
    - 相关导航和 tab 组件
  - 这一步先不做什么：不开始做事务视图对象面板。
  - 怎么算完成：
    1. 顶层模式和内部 tab 语义清楚
    2. 不再把事务视图误挂在旧 center tab 下
  - 怎么验证：
    - 桌面端导航交互测试
  - 对应需求：`requirements.md` 需求 1、需求 3、需求 8
  - 对应设计：`design.md` §2.1、§2.2、§6.1

### 阶段检查

- [x] 2.3 代码视图零破坏检查
  - 状态：DONE
  - 这一步到底做什么：检查代码视图是不是已经稳定收口，不再被后续事务视图施工拖着走。
  - 本次检查结果：代码视图仍走原 `WorkbenchLayout + Outlet` 主链路；本轮没有改消息时间线、会话树、Git 主逻辑和终端主逻辑。
  - 做完你能看到什么：可以放心开始做事务视图主骨架。
  - 先依赖什么：2.1、2.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
  - 主要改哪里：本阶段全部相关文件
  - 这一步先不做什么：不加事务对象新能力。
  - 怎么算完成：
    1. 代码视图回归通过
    2. 旧入口职责已收紧
  - 怎么验证：
    - 回归测试
    - 人工走查
  - 对应需求：`requirements.md` 需求 3
  - 对应设计：`design.md` §2.2、§6.1

---

## 阶段 3：搭事务视图主骨架

- [x] 3.1 建立事务视图左中右三栏布局
  - 状态：DONE
  - 这一步到底做什么：新建事务视图根容器，把左侧导航、中间主对象区、右侧详情与 AI 区三块位置正式立起来。
  - 本次实现结果：已把事务视图正式拆成“左侧事务菜单 + 中间主对象区 + 右侧辅助区”三栏；事务模式下直接复用原左侧边栏壳，但边栏内容已从代码视图的工作区 / 会话树切成事务菜单，并按当前分区显示收藏 / 标签、代办来源或自动化任务。右侧详情与助手标签也已挂到同一事务 Provider 作用域内，避免左右区状态脱节。代码视图继续通过 `CodeWorkbenchView` 走原 `Outlet`，没有碰消息流和工具主链路。
  - 做完你能看到什么：事务视图不再是一个概念，而是一套可见的正式布局骨架。
  - 先依赖什么：2.3
  - 开始前先看：
    - `requirements.md` 需求 4、需求 8
    - `design.md` §2.2、§2.3.2、§3.1、§4.1
    - 前端页面与样式规范
  - 主要改哪里：
    - `apps/user-app` 新增事务视图组件
    - 事务视图样式和布局文件
    - `apps/user-app/src/shared/i18n/index.ts`
  - 这一步先不做什么：不先接文档库真实数据，不接待办和自动化细节逻辑。
  - 怎么算完成：
    1. 事务视图三栏结构可见
    2. 左中右区职责清楚
  - 怎么验证：
    - 组件渲染测试
    - 桌面端视觉走查
  - 对应需求：`requirements.md` 需求 4、需求 8
  - 对应设计：`design.md` §2.2、§3.1、§4.1、§6.2

- [x] 3.2 建立事务视图顶部工具区
  - 状态：DONE
  - 这一步到底做什么：在事务视图里补顶部工具区和可展开区，给搜索、新建、筛选、插件快捷入口一个正式位置。
  - 本次实现结果：已新增 `AffairsToolbarTray`，包含当前分区标题、对象计数、展开/收起工具区，以及新建 / 搜索 / 筛选的正式占位入口；展开态同时展示当前工作区对象摘要。
  - 做完你能看到什么：事务视图不再只有三栏外壳，常用动作也有稳定入口。
  - 先依赖什么：3.1
  - 开始前先看：
    - `requirements.md` 需求 7
    - `design.md` §3.1、§5.3
    - 前端页面与样式规范
  - 主要改哪里：
    - 事务视图顶部工具区组件
    - 样式和交互状态
  - 这一步先不做什么：不把全部插件真实接通，只先把正式位置和交互壳立住。
  - 怎么算完成：
    1. 顶部工具区常驻项已可见
    2. 展开/收起行为明确
  - 怎么验证：
    - 交互测试
    - 视觉走查
  - 对应需求：`requirements.md` 需求 7
  - 对应设计：`design.md` §3.1、§5.3

- [x] 3.3 把现有助手入口收口为事务视图通用 AI 助手
  - 状态：DONE
  - 这一步到底做什么：保留现有 Butler/助手底层逻辑，但把事务视图右侧 AI 区做成正式通用助手入口，不再要求用户先跳独立 Butler 页面。
  - 本次实现结果：已通过 `UniversalAssistantBridge` 复用 `ButlerRuntimeStore`、`MessageTimeline`、`ComposerPanel`、`PermissionRequestList`；事务视图右侧助手会自动带上当前对象标题、摘要和来源，默认语境已经切成文档整理、待办拆解、浏览器任务和导出建议。
  - 做完你能看到什么：事务视图右侧助手能围绕文档、标签、待办、自动化、浏览器和运维等通用对象工作。
  - 先依赖什么：3.1、3.2、3.3、3.3.1
  - 开始前先看：
    - `requirements.md` 需求 8、需求 8.1
    - `design.md` §2.2、§2.3.2、§3.1、§6.4
    - `spec013.1`
    - `spec013.5`
  - 主要改哪里：
    - 事务视图右侧辅助区组件
    - 助手上下文桥接层
    - 相关 i18n 和空态说明
  - 这一步先不做什么：不重造一套新的助手后端，不删除现有 Butler 页面。
  - 怎么算完成：
    1. 事务视图右侧 AI 助手可见
    2. 助手上下文已经围绕当前事务对象，而不是继续只认代码会话
  - 怎么验证：
    - 事务视图对象上下文联动测试
    - 人工回放通用助手链路
  - 对应需求：`requirements.md` 需求 8、需求 8.1
  - 对应设计：`design.md` §2.2、§2.3.2、§3.1、§6.4

- [x] 3.3.1 对齐事务视图与代码视图的共享 UI 语言
  - 状态：DONE
  - 这一步到底做什么：把事务视图的共享壳层、顶部按钮区、中间主区顶部栏、右侧详情/AI 顶部栏全部对齐到代码视图现有工作台节奏。
  - 本次实现结果：顶层 `代码 / 事务` 继续挂在左侧原壳层位置；左侧菜单按钮、右侧标签页、顶部工具区、面板边框和间距全部改为复用代码视图已有 UI 语言；右侧不再堆卡片，而是对齐文件 / Git / 进程那套标签页结构，收成 `对象详情 / 助手对话` 两个标签。
  - 做完你能看到什么：切换代码 / 事务时，用户会感觉是在同一个应用里换模式，而不是换了一个产品。
  - 先依赖什么：3.1、3.2
  - 开始前先看：
    - `requirements.md` 需求 7.1
    - `design.md` §1.4、§9
    - `docs/20260529-事务视图页面结构草图.md`
  - 主要改哪里：
    - 事务视图共享壳层组件
    - 中间主区顶部栏
    - 右侧详情 / AI 顶部栏
    - 相关 i18n 和样式令牌
  - 这一步先不做什么：不另外发明一套事务视图专属标题栏风格，不重做全局主题。
  - 怎么算完成：
    1. 左上角模式切换和顶部通用按钮在双视图里位置一致
    2. 中间主区顶部栏高度与代码视图标题栏一致
    3. 右侧详情 / AI 顶部栏高度也一致
  - 怎么验证：
    - 视觉对比走查
    - 组件快照或截图测试
  - 对应需求：`requirements.md` 需求 7.1
  - 对应设计：`design.md` §1.4、§9

### 阶段检查

- [x] 3.4 事务视图骨架检查
  - 状态：DONE
  - 这一步到底做什么：确认事务视图已经是正式骨架，而不是空壳布局。
  - 本次检查结果：事务视图已经不是空壳。三栏、顶部工具区、详情面板和事务助手都已落位；事务模式下左栏已不再显示代码会话树；事务右栏和助手上下文已统一挂在 Provider 内；`pnpm --dir apps/user-app build` 已通过。
  - 做完你能看到什么：可以继续把文档库和其他对象逐步接进来。
  - 先依赖什么：3.1、3.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
  - 主要改哪里：本阶段全部相关文件
  - 这一步先不做什么：不扩待办、自动化具体业务。
  - 怎么算完成：
    1. 三栏和顶部工具区稳定
    2. AI 辅助区定位清楚
  - 怎么验证：
    - 人工走查
    - 组件测试
  - 对应需求：`requirements.md` 需求 4、需求 7、需求 8
  - 对应设计：`design.md` §2.2、§3.1、§6.2

---

## 阶段 4：把文档库作为第一优先对象接进来

- [x] 4.1 在事务视图中接入文档库默认首页
  - 状态：DONE
  - 这一步到底做什么：让事务视图默认显示文档库，而不是只停在空态或占位说明。
  - 本次实现结果：事务视图默认分区已经固定为文档库；中间主区会基于当前工作区会话数据生成首批文档对象列表；右侧详情区默认展示选中文档摘要和预览。
  - 做完你能看到什么：用户进入事务视图后，第一眼就能操作文档库。
  - 先依赖什么：3.3
  - 开始前先看：
    - `requirements.md` 需求 5
    - `design.md` §2.3.2、§3.3、§4.1
    - 文档库相关现有实现和后续接入方案
  - 主要改哪里：
    - `apps/user-app` 事务文档库容器
    - 文档库接入层
    - 相关 i18n 和空态文案
  - 这一步先不做什么：不要求一次性把文档库所有功能重构完。
  - 怎么算完成：
    1. 事务视图默认主区为文档库
    2. 至少能展示基础列表/主视图
  - 怎么验证：
    - 人工回归主链路
    - 组件/集成测试
  - 对应需求：`requirements.md` 需求 5
  - 对应设计：`design.md` §2.3.2、§3.3、§4.1、§6.2

- [x] 4.2 打通左侧标签/收藏与中间文档库主区联动
  - 状态：DONE
  - 这一步到底做什么：让事务视图左侧导航真正控制中间文档库，而不是只摆几个静态入口。
  - 本次实现结果：文档库分区下，左侧已经按事务菜单风格收成“总览 / 收藏 / 标签”三组，收藏和标签节奏继续向参考页靠拢；中间列表和右侧详情会跟随左侧切换。代办分区下已经把 Butler 收件箱事项和会话跟进任务分开标清来源，并把来源信息带到主列表和详情区。自动化分区下已收紧成更轻的信息密度，列表优先展示状态、触发方式、目标会话和最近执行摘要，右侧继续看最近运行结果。
  - 做完你能看到什么：左侧点标签、收藏、最近项，中间主区和右侧详情会跟着变。
  - 先依赖什么：4.1
  - 开始前先看：
    - `requirements.md` 需求 5、需求 8
    - `design.md` §3.2、§4.1、§4.2
  - 主要改哪里：
    - 事务视图左栏和主区联动逻辑
    - 右侧辅助面板上下文逻辑
  - 这一步先不做什么：不继续扩待办和自动化业务。
  - 怎么算完成：
    1. 左中右最小联动成立
    2. 右侧详情不再脱离当前对象
  - 怎么验证：
    - 集成测试
    - 人工联动回放
  - 对应需求：`requirements.md` 需求 5、需求 8
  - 对应设计：`design.md` §3.2、§4.1、§4.2、§6.2

### 阶段检查

- [x] 4.3 文档库首链路检查
  - 状态：DONE
  - 这一步到底做什么：只检查事务视图 + 文档库这条第一主链路是否跑通。
  - 本次检查结果：`/workspaces/:workspaceId/affairs` 已能直接进入事务视图；默认主区是文档库；左中右联动已经成立；构建验证通过。
  - 做完你能看到什么：事务视图已经不是空壳，而是有第一条真实工作链路。
  - 先依赖什么：4.1、4.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
  - 主要改哪里：本阶段全部相关文件
  - 这一步先不做什么：不在这一步继续塞待办、自动化和插件实现。
  - 怎么算完成：
    1. 事务默认首页可用
    2. 左中右文档库联动可用
  - 怎么验证：
    - 人工走查
    - 文档库首链路测试
  - 对应需求：`requirements.md` 需求 4、需求 5、需求 8
  - 对应设计：`design.md` §2.3.2、§4.1、§6.2

---

## 阶段 5：给待办、自动化、插件和移动端收口

- [x] 5.1 给待办、自动化、插件补正式入口和占位态
  - 状态：DONE
  - 这一步到底做什么：把待办、自动化、插件入口放进事务视图正式结构里，就算首阶段只做占位，也把位置和归属定住。
  - 本次实现结果：待办和自动化已经不再只是占位入口，而是接上 Butler 现有数据：代办会汇总助手收件箱事项与会话跟进任务，自动化会显示助手自动化任务与最近执行结果；当前只保留文档库 / 代办 / 自动化三个正式分区，不再额外长第四套事务入口。
  - 做完你能看到什么：事务视图已经是一个通用事务容器，不再只是文档库页。
  - 先依赖什么：4.3
  - 开始前先看：
    - `requirements.md` 需求 6
    - `design.md` §2.2、§3.2
  - 主要改哪里：
    - 事务视图左栏、主区入口
    - 事务视图空态和占位说明
  - 这一步先不做什么：不在本阶段把所有业务都做完整。
  - 怎么算完成：
    1. 入口位置明确
    2. 占位态清楚说明后续会接什么
  - 怎么验证：
    - 页面走查
  - 对应需求：`requirements.md` 需求 6
  - 对应设计：`design.md` §2.2、§3.2

- [x] 5.2 补移动端和小屏降级方案
  - 状态：DONE
  - 这一步到底做什么：给双视图结构补移动端和小屏规则，定义事务视图在抽屉、分段或单列下怎么活。
  - 本次实现结果：移动端顶层模式已支持 `代码 / 事务` 状态恢复；当事务视图激活时，移动端工作台入口优先回到 workspace 主舞台；事务视图 CSS 已补两档小屏降级，1180px 下先折成两列，820px 下再收成单列，避免硬挤桌面三栏。
  - 做完你能看到什么：不是只有桌面端能切，移动端也知道怎么进、怎么退、怎么切对象。
  - 先依赖什么：5.1
  - 开始前先看：
    - `requirements.md` 需求 9
    - `design.md` §5.3、§6.3
    - `spec009`
    - `spec009.1`
  - 主要改哪里：
    - 移动端导航与事务视图降级组件
    - 相关样式、路由和状态恢复逻辑
  - 这一步先不做什么：不强行复刻桌面三栏。
  - 怎么算完成：
    1. 移动端进入双视图路径清楚
    2. 事务视图在小屏下有正式降级结构
  - 怎么验证：
    - 移动端页面测试
    - 手工切换验证
  - 对应需求：`requirements.md` 需求 9
  - 对应设计：`design.md` §5.3、§6.3

### 最终检查

- [x] 5.3 最终检查点
  - 状态：DONE
  - 这一步到底做什么：确认 spec016 已经达到“能指导真实实现并能持续回写”的标准，而不是只停在概念图层。
  - 本次检查结果：spec016 的需求、设计、任务和当前实现已重新对齐；`tasks.md` 不再停在 Draft；当前剩余主要是后续真实文档索引、待办业务和自动化能力继续深化，不属于这轮骨架改造缺口。
  - 做完你能看到什么：需求、设计、任务、验收路径能一一对上，后续实现可以按阶段推进。
  - 先依赖什么：5.1、5.2
  - 开始前先看：
    - `requirements.md`
    - `design.md`
    - `tasks.md`
    - `docs/`
  - 主要改哪里：当前 Spec 全部文件
  - 这一步先不做什么：不再追加新需求。
  - 怎么算完成：
    1. 关键阶段和边界都能追踪到需求与设计
    2. 风险和待确认项已写清楚
    3. 后续接手的人第一眼能知道先从哪里动手
  - 怎么验证：
    - 按 Spec 验收清单逐项核对
  - 对应需求：`requirements.md` 全部需求
  - 对应设计：`design.md` 全文
