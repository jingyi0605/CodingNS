# 补充文档目录（spec008）

`docs/` 用于存放 `spec008-桌面端与H5交付增强` 的补充材料。

建议优先补齐以下文档：

- `api.md`：客户端启动、连接探测、更新检查相关接口约定
- `integration.md`：桌面端与 H5 的联调步骤和排障指引
- `acceptance-checklist.md`：验收清单（连接、重连、升级、回退）
- `acceptance-result.md`：阶段验收结果与证据链接
- `release-process.md`：桌面分发、签名、升级与回退流程

当前如果还没有补充材料，可以先保留本文件，后续按任务推进逐步补全。
