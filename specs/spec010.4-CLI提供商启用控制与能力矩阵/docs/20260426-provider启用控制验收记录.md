# 20260426-provider启用控制验收记录

## 当前结论

自动化回归已经覆盖到阶段 4 的前端收口，结果是通过。

还没做的是“真实界面手工点击验收”，原因不是实现没写完，而是本轮没有按项目规则主动启动前后端服务。

一句人话：

代码和自动化已经收住了，差的是最后一轮真人点界面确认。

## 本轮已验证内容

### 前端自动化

已通过命令：

```bash
pnpm --filter user-app exec vitest run \
  src/settings/ProviderManagementPanel.test.tsx \
  src/features/settings/pages/SettingsPage.test.tsx \
  src/features/conversation/components/SessionProviderPicker.test.tsx \
  src/features/conversation/components/ConversationSelectionActions.test.tsx \
  src/features/conversation/components/ParallelSessionCreateModal.test.tsx \
  src/features/conversation/components/ComposerPanel.test.tsx
```

覆盖到的点：

1. 设置页 provider 管理面板可以加载 catalog、显示能力矩阵、切换启用态
2. 桌面端和移动端设置页都已经挂上 provider 管理入口
3. 新建会话、选区新会话、并行会话、Fork 相关 provider 列表都会过滤 disabled provider
4. `SessionProviderPicker` 的 capability cache 在设置切换后会被清理并重新请求

### 后端门禁

本轮没有重新把 host 相关集成测试再跑一遍，但阶段 1 到阶段 3 的任务条目已经分别记录过对应测试覆盖。

当前可以确认的事实：

1. `tasks.md` 里的 1.1 到 3.2 都已经按条目标成 `DONE`
2. 对应条目里已经写明各自的 host 集成测试入口
3. 当前阶段 4 改动没有再去修改后端门禁规则，只是在前端补统一可见性、设置页和缓存失效

## 仍待人工确认的项目

下面这些需要用户明确允许后，启动真实界面再点一遍：

1. 设置页里禁用 provider 后，新建会话、Fork、并行会话、Butler、Skill 页面即时变化是否符合预期
2. 重新启用后，旧会话是否按设计重新出现
3. Butler 和 Skill 在真实数据下的旧绑定解释是否清楚

## 建议的下一步

如果要正式收尾这个 Spec，下一步就做一件事：

按回归清单启动一次手工验收，然后把结果补回 `tasks.md` 的 `5.2`。
