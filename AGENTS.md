# 项目全局规则

## 术语定义

- **办公目录**：指 `~/WorkFile`。除非另有说明，提到"工作目录"时一律指该路径。

## 沟通规则

1. 始终使用中文和用户沟通和进行思考。
2. 表达直接、清楚、少废话，不要故作高深。
3. 如果一句话能说清，就不要堆三句术语。

## Spec 文档规则

后续只要在 `specs/` 目录里新建或修改 Spec，必须遵守下面这些规则。

### 1. 文档必须写成人话

- 文档首先是给人看的，其次才是给工具追踪的。
- 不要写“能力路由抽象层闭环”“服务治理底座”“多维能力编排”这种空话，除非后面立刻用一句人话解释它到底是什么。
- 每一段都应该让一个刚接手项目的人看懂“这是要干什么”。

### 2. 任务文档必须优先回答这几个问题

每个任务至少要让人一眼看明白：

1. 这一步到底做什么
2. 做完以后能看到什么结果
3. 这一步依赖什么
4. 主要改哪些文件
5. 这一步明确不做什么
6. 怎么验证是不是真的做完了

如果这些问题答不清，这个任务就不合格。

### 3. 允许保留追踪信息，但不能拿追踪信息当正文

- 可以写“对应需求”“对应设计”
- 但不能整段都在堆 `requirements.md`、`design.md`、章节号、术语名
- 追踪信息是辅助，不是主体

### 4. 避免黑话和假大空表述

禁止优先使用这类写法：

- 闭环
- 抽象层
- 赋能
- 治理
- 编排
- 底座
- 能力沉淀

如果确实要用，必须马上补一句人话解释，不然就换成普通表达。

### 5. 推荐写法

优先使用下面这种表达：

- “这一步把什么东西建出来”
- “做完以后页面/接口/数据会发生什么变化”
- “先不做什么，避免范围失控”
- “别人接手时第一眼该看什么文件”

### 6. 模板也必须说人话

- `specs/000-Spec规范/Spec模板/` 下的模板必须保持人话风格
- 后续创建新 Spec 时，默认沿用人话模板
- 如果模板变回黑话，优先修模板，不要把垃圾继续复制到新 Spec

## 前端代码编写原则
1. 不要编辑编译产物，js文件不要添加到git
2. 新建前端页面的时候，页面的文案不要采用工程师视角的语言，按照最终交付给普通用户的标准编写文案
3. 必须使用统一的i18n字典，不允许使用硬编码的显示文字
4. 新增和修改前端代码，必须修改user-app，不允许再改已经下线的历史前端目录
5. 只要新增或修改前端页面、分组区块、表单、列表、空态、按钮样式，开始前必须先阅读：
   - [docs/开发设计规范/20260419-前端页面与样式设计规范.md](/Users/jackson/Code/CodingNS/docs/开发设计规范/20260419-前端页面与样式设计规范.md)
6. 只要新增或修改桌面端模态框、移动端 sheet、模态框内部按钮和结构，开始前必须先阅读：
   - [docs/开发设计规范/20260419-模态框与按钮设计规范.md](/Users/jackson/Code/CodingNS/docs/开发设计规范/20260419-模态框与按钮设计规范.md)
   - [specs/spec008.2-桌面端与移动端模态框统一模板/docs/20260418-模态框迁移与新增约束清单.md](/Users/jackson/Code/CodingNS/specs/spec008.2-桌面端与移动端模态框统一模板/docs/20260418-模态框迁移与新增约束清单.md)
7. 如果需求同时涉及“页面样式”与“弹窗样式”，两份规范都要先看，不能只看其中一份

## 后台任务规则
1. 后续只要新增、修改、重构后台任务，或者改动工作台刷新、watcher、helper、订阅推送、缓存失效这些后台链路，必须先阅读并遵守：[specs/spec001.2-后端任务调度与主线程压力治理/20260412-后台任务接入规范.md](/Users/jackson/Code/CodingNS/specs/spec001.2-后端任务调度与主线程压力治理/20260412-后台任务接入规范.md)
2. 这不是“有空再看”的参考文档，而是后台任务开发的前置步骤；如果还没读这份规范，不允许继续往下设计或写代码
3. 不允许绕开现有 `TaskManager` 体系重新长出新的 `inflight`、私有 `timer`、私有重试队列，除非文档里明确说明这个任务不属于全局后台任务
4. 后续凡是新增后台任务，必须按规范自查这些问题：
   - 读接口是否纯读，刷新入口是否显式
   - watcher 是否只打脏标记
   - 同一资源同类任务是否只有一个 inflight
   - 轮询是否由状态变化驱动
   - 订阅链路是否支持重放当前快照
   - helper 是否懒启动、可回收、可重连
   - Host 收尾是否还残留同步大事务或重 SQLite
5. 如果需求里包含“后台任务”“定时刷新”“扫描”“订阅”“helper”“工作台刷新”“Git/Terminal 刷新”这些关键词，默认先回到上面的规范文档检查，再继续实现

## 仓库代码提交规则
1. 如果你认为当前修改的代码已经满足git提交的最小模块，应该主动提出提交代码
2. 提交代码的时候根据功能、模块、文档分配提交，不允许混合提交
3. 提交信息的格式要求：（类型：spec编号-内容；），修复类或者临时修改，没有spec编号的，不体现编号，不要写“无编号”

## Android 打包规则
1. 如果需要打包 Android 应用程序，先阅读：[docs/使用说明/20260331-Android应用打包说明.md](/Users/jackson/Code/CodingNS/docs/使用说明/20260331-Android应用打包说明.md)
2. 日常 Android release 打包统一走 `bash scripts/build-android.sh release`，不要直接把 `tauri android build` 当成稳定入口
3. 发布给用户前，先确认产物是 `app-universal-release.apk`，不要把 `debug` 或 `unsigned` 包当成正式包
